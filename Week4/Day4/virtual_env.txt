1. venv [in terminal] -python3 -m venv my_venv
2. activate my_venv [in terminal inside the folder where venv]
    source my_venv/bin/activate

3. create requirements.txt: [in terminal] pip freeze > requirments.txt

4. install site-package: [in terminal] pip install psycopg2 / psycopg2-binary

5. update requirments pip freeze > requirments.txt


6. 

